import Section from "../components/section";

export default function AboutPage() {
  return (
    <Section title="About">
      <p>
        This is a Single Page Application using React Router. Links update the URL and
        swap components without reloading the page.
      </p>
    </Section>
  );
}
